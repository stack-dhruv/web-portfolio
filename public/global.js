// // scrolling event handler
// const handleScroll = () => {
//     var divs = document.getElementsByTagName("section");

//     for (var i = 0; i < divs.length; i++) {
//         var rect = divs[i].getBoundingClientRect();
//         var isVisible = rect.top >= 0 && rect.bottom <= window.innerHeight;

//         if (isVisible) {
//             console.log("Visible div: " + divs[i].id);
//             // You can perform further actions based on the visible div
//             break; // If you want to find only the first visible div
//         }
//     }
// };

// window.addEventListener("scroll", handleScroll);
